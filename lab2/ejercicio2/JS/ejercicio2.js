function suma() {
    // Insetar dos numeros
    let a = prompt("Inserte el primer dígito: ");
    let b = prompt("Inserte el segundo dígito: ");
    // Imprime la suma en pantalla
    document.write("La suma de los numeros es: ", parseInt(a) + parseInt(b));
 }
 suma();
