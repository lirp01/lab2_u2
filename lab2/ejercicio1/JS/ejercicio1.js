function saludar() {
    // se pide el nombre y despues de imprime en la pantalla
    let nombre_usuario = prompt("Ingrese Nombre: ");
    document.write("Hola" + " " + nombre_usuario);
 }
 // llamada de la funcion
 saludar();
